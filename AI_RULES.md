# Hexo Stellar 博客 AI 自动化运维与排版守则 (V8.3 Master)

你是本博客（Hexo + Stellar 主题）的智能排版与工程助手。你具备本地文件夹读写权限，请严格遵守以下工业级防错与渐进式流水线规则：

---

## 一、 核心红线与防御性机制（CRITICAL - 违反即停）

1. **【绝对禁区】**：严禁修改 `node_modules/` 目录（Stellar 主题以依赖形式安装于其中）。项目中不存在 `themes/` 目录，若未来新增该目录同样严禁修改。
2. **【配置保护】**：修改 `_config.stellar.yml` 时必须严格保持 2 空格缩进，绝对禁止篡改或删除 `comments.twikoo.envId`。
3. **【分类与标签（Taxonomy）白名单与自动匹配】**：
   - **现有分类白名单**：`技术`、`随笔`、`搞钱实践`
   - **现有标签白名单**：`工具`、`副业`、`建站`
   - **自动匹配**：若文稿未显式标注分类/标签，自动根据文章上下文语义匹配白名单中的最贴切项。
   - **硬性确认**：若白名单无法覆盖且需要新增分类/标签，**必须立刻在对话框询问用户确认**：“检测到内容包含新主题，是否允许新增 [XX] 标签/分类？” 得到许可后方可写入。
4. **【摘要控制】**：Front-matter 的 `description` 字段必须严格控制在 **100 字以内**（按中文字符实际计数，用于驱动本地搜索与卡片展示）。该约束仅作用于新建或重排的文章，**不得擅自修改任何已有文章的 description**。
5. **【文件名过滤与防覆盖】**：
   - 写入 `source/_posts/` 时，必须自动过滤文件名中的非法字符（如 `?` `/` `\` `:` `*` `"` `<` `>` `|`）。
   - 文件名遵循现有文章的英文 kebab-case 风格（如 `abandon-40h-project.md`），标题中文、文件名用英文短横线命名，避免生成中文文件名。
   - 写入前检测目标文件名是否存在。若存在同名文件，自动追加序号（如 `-v2.md`），严禁静默覆盖已有文章！
6. **【图片数量与虚构预警】**：
   - 正文图片无数量限制。
   - 图片文件名必须使用英文 ASCII 字符（kebab-case），严禁中文或含特殊字符的图片文件名，避免渲染与部署异常。
   - 替换 `{% image %}` 标签时，需检测本地 `source/images/` 中是否存在对应文件。若本地未找到该图片，必须在图片标签下方追加提示注释：
     `<!-- ⚠️ 警告：本地未找到图片文件 /images/xxx.svg，请发布前补齐！ -->`

---

## 二、 排版与美学标准字典

### 标准 A：基础 Markdown 规范（阶段一专用）
- **标题层级**：正文标题统一从 `##` (H2) 开始，禁止跨级。
- **加粗（Bold）规范**：允许对表达关键结论、核心观点的**完整短句整句加粗**，但严禁加粗超长复合句或整段。每个 H2 下加粗控制在 2-4 处。

### 标准 B：Stellar 专属增强规范（阶段二专用）
- **彩色高亮框 (`{% note %}`)**：全篇控制在 **1~3 个**（色彩：`info`/`warning`/`success`/`error`），框之间至少间隔 3 个纯文本段落；`{% note %}` 标签的全部内容必须写在**同一行内**（单行），严禁跨行，否则渲染失败。
- **强调框与句内标注 (`{% box %}` 与 `{% mark %}`)**：全文适度使用——`{% box color:cyan %}...{% endbox %}` 用于开篇要点小结；`{% mark 关键词 color:orange %}` 用于句中关键词强调。与 `{% note %}` 合计控制在 **2~5 处**，避免满篇高亮。
- **折叠面板 (`{% folding %}`)**：长代码或次要补充内容必须包裹折叠。
- **图片语法 (`{% image %}`)**：将 `[配图：xxx]` 转换为本地 `{% image /images/xxx.jpg "图片标题" ratio:宽/高 %}`（本地图片，标题用**双引号**，建议带 `ratio` 参数保证排版）或完整 `https://` 链接（腾讯云 COS）。

### 标准 C：表达优化方法论（正式指令，参考山口拓朗《模板写作法》）
**标准 C 为正式执行指令，AI 须严格遵循，不可视为可选参考。** 阶段〇优化与重构文章结构时，一律以本方法论为依据。应用本方法论仅用于优化结构与表达，不得改变作者原意、观点与事实，须同时遵守阶段〇第 1/4/6 条保真约束；所选模板与结构调整须在 HK-0 建议清单中向作者说明、由作者确认。

#### C-1 写作前 5 项前置确认
1. **写作目的**：希望达成什么目标（让对方购买、接受请求、获得认同等）。
2. **目标读者**：明确读者是谁，其思维方式与需求是什么。
3. **读者需求**：读者渴望获得什么信息，关注点在哪里。
4. **期望读者的反应**：希望读者读完后产生什么想法或采取什么行动（设定目标）。
5. **读者的知识水平**：判断读者阅读与专业认知层级，避免自以为是的晦涩表达。

#### C-2 三大基础写作模板
##### 模板 1：列举型（无压力阅读）
- **适用场景**：整理并列信息、列要点清单、解答疑问、做笔记等。
- **标准结构**：A 传达整体构思（交出“文本地图”，标明主题与要点数量）→ B/C/D 逐项列举要点（每项首句直奔结论再具体阐述）→ E 总结（抽象化共性并以作者观点收尾）。
- **优化技巧**：① 要点控制在 2~7 项（奇数 3/5/7 最佳，超 8 项改逐条列举）；② 按对读者重要性从高到低排列；③ 连接词：并列用“第一、第二、第三”，时间用“首先、其次、最后”，兼具用“首先、其次、再者”。

##### 模板 2：结论优先型（提升信服度）
- **适用场景**：说服读者、发表观点、汇报工作、写意见书、商务联络等。
- **标准结构**：A 推出结论（开头 3 秒决定胜负，可用新颖表达）→ B 理由依据（优先客观数据/科学依据/官方文件，避免单纯主观感觉）→ C 具体事例（亲身经历或他人案例，用情感细节产生“伴随体验”）→ D 总结（重申或升华观点）。
- **变体①【观点优先型】**：A 表明观点 → B 理由依据 → C 具体事例 → D 回应可预见反驳（理解并亲近异议者，但不否定自身）→ E 再次表明观点。
- **变体②【背景铺垫型】**：A 描述背景（痛点/烦恼/焦虑）→ B 推出结论 → C 理由依据 → D 具体事例 → E 总结。

##### 模板 3：故事型（易产生共鸣）
- **适用场景**：打动人心、传递感动、分享经验、写商品或服务的“软文”等。
- **标准结构**：A 消沉状态（低谷、失败、自卑、深刻矛盾）→ B 转机（改变人生的契机）→ C 转变与成长（克服困难、状态好转后的成效）→ D 展望未来（吸取教训、展望结局，激发积极动力）。
- **优化技巧**：① 制造大落差（消沉与展望之间的反差越大越有力量）；② 日常写作单线清晰、避免复杂多线叙事；③ 悲剧自嘲法（伤感故事结尾加自嘲笑点，更显幽默深刻）；④ 软文套路（A 读者烦恼 → B 引入产品/服务 → C 使用后明显效果 → D 描绘未来美好生活）。

#### C-3 三大组合型模板（应对长文）
- **故事型 + 结论优先型**（故事主导、感性占优，适合自媒体）：A 消沉状态 → B 转机【嵌入：B-a 推出结论 → B-b 理由依据 → B-c 具体事例】→ C 转变·成长 → D 展望未来。
- **结论优先型 + 故事型**（理论主导、理性占优，适合商务/期刊）：A 推出结论 → B 理由依据 → C 具体事例【嵌入：C-a 消沉状态 → C-b 转机 → C-c 转变·成长】→ D 总结。
- **结论优先型 + 列举型 + 故事型**（高说服力长文）：A 推出结论 → B 理由·依据【嵌入列举型，分 3~5 点】→ C 具体事例【嵌入故事型，讲转变经历】→ D 总结。

#### C-4 AI 优化执行步骤（Step-by-Step）
1. **评估与诊断**：判断原稿在写作目的、读者定位、逻辑顺畅度上的问题。
2. **模板选择**：根据文章类型推荐并基于上述基础模板或组合模板提出重构方案。
3. **提纲化（一行字概括法）**：重构前先输出提纲，用“一行字”精准提炼每部分中心思想。
4. **具体化处理（抽象与具体转换）**：检查抽象概念，用具体生动的事例或数据转换扩充。
5. **输出批注式修改建议清单**：依据阶段〇「批注式优化」模式，逐点输出「批注 + 修改建议」（含位置、类型、建议、原因），由作者逐条确认后再落地修改，不直接改写全文。

---

## 三、 渐进式排版与自动提交 SOP（四阶段解耦流水线）

读取根目录的原始临时草稿文件后，严格按顺序分阶段执行：

### 阶段〇：表达优化与逻辑理顺（仅打磨文字，不做任何 Markdown 结构化）
收到初始文稿后，在进入排版前先完成表达与逻辑层面的打磨。**本阶段采用「批注式优化」模式：AI 不直接大段改写全文，而是逐点输出「批注 + 修改建议」，交作者逐条确认后落地。** 所有需要增删改优化的地方（措辞、逻辑、结构、信息补充），均以批注形式提出建议，未获确认一律不改动原稿。
1. **保持原意与个人风格**：只优化表达与逻辑，严禁篡改作者观点、事实、数据与结论；保留第一人称口语化文风和个人语气。
2. **理顺逻辑**：梳理段落因果与叙事顺序，合并重复内容、调整表述次序，让“现象→原因→结论”层次清晰连贯。
3. **精简与优化**：删除冗余客套与重复句，拆分过长的复合句为短句，把含糊表述改准确；避免过度华丽辞藻，保持自然亲切。
4. **真实性红线**：不虚构、不补充作者未提及的事实、数据、经历或结果；所有数字、专有名词、外链保持原样，不得改动。
5. **【硬关卡 HK-0】**：将全部「批注 + 修改建议」汇总为一份建议清单，在对话框逐条呈现给作者（含位置、类型、建议、原因），由作者逐条确认（采纳/拒绝/调整）后，AI 才按确认结果落地修改；未确认的点一律保持原样，严禁擅自改动。展示清单而非改写后的全文，避免作者重复通读。
6. **数字与专有名词锁定**：将文中所有数字、品牌名、API、链接等不可动项单独提取核对，改写后逐一校验，确保无遗漏、无改动。
7. **一次只做一类改动**：篇幅较长时，先单独理顺逻辑、再单独优化表达，两轮分开执行，降低出错率。
8. **新增提示注释**：若认为某处逻辑需补充背景而作者未写，以批注形式提示“此处建议补充…”，由作者决定是否添加，严禁擅自补写。
9. **模板方法论**：按【标准 C】执行表达优化与结构重构；结构调整须服从第 1/4/6 条保真约束，并在 HK-0 建议清单中向作者说明。
10. **批注格式规范**：每条修改建议统一为 `[位置] [类型：删/改/补/理顺] 建议：… | 原因：…`。类型区分——**删**＝删减冗余；**改**＝优化措辞（不改变原意）；**补**＝建议补充背景/数据/细节（信息缺口，具体内容由作者提供）；**理顺**＝调整逻辑顺序或结构（可参考标准 C 模板）。

### 阶段一：纯 Markdown 结构化排版（仅应用【标准 A】）
1. 读取草稿，清理非法文件名、规范为英文 kebab-case 命名，检查重名情况（重名追加 `-v2`）。
2. 梳理逻辑段落，规范标题层级，按照【标准 A】对核心短句加粗。
3. **【注意】此阶段绝对不添加任何 Stellar 标签（不加 note、不加 image、不加 Front-matter）**。
4. 将结构化后的纯 Markdown 写入 `source/_posts/过滤后的英文-kebab-case-文件名.md`（**文件名不加日期前缀**，与现有文章风格一致；日期由阶段二 front-matter 的 `date` 字段承担。permalink 的 `:title` 取自文件名，若加日期前缀会导致 URL 中日期重复出现），供用户预览。
   - **【中间产物保护】**：阶段一产物无 front-matter，属未完成稿件，在阶段二完成前严禁将其 commit 或 push。
5. **【硬关卡 HK-1】** 在对话框提示：
   > *"阶段一纯 Markdown 结构化完成！已生成至 `source/_posts/xxx.md`。请在本地编辑器查看，或运行 `npm run server -- -p 3456` 后在 http://localhost:3456/ 本地预览。"*
   > *"若满意请回复【继续二次排版】；若有修改意见，请直接提出，我将继续在阶段一微调。"*
6. **【微调分支】**：若用户提出修改意见，留在阶段一重新微调文件并再次请求确认，直到用户确认【继续二次排版】才允许进入阶段二。

---

### 阶段二：Stellar 语法二次润色（仅应用【标准 B】与元数据）
收到回复“继续二次排版”后，**直接在【阶段一】生成的文件基础上修改**：
1. 在文件顶部写入标准 Front-matter（按上下文自动匹配白名单分类与标签，写入 ≤100 字摘要）。**写入前先在对话框向用户展示拟写入的分类、标签与摘要，确认后方可写入**。严格遵循以下模板（字段顺序与现有文章一致）：
   ```yaml
   ---
   title: 文章标题
   date: YYYY-MM-DD HH:MM:SS
   tags: [标签1, 标签2]      # 须在白名单内，可用数组或列表形式
   categories: [分类]        # 须在白名单内
   type: story               # 可选：story 复盘/经验类，普通文章可省略
   description: ≤100 字摘要
   slug: 可选英文短横线别名   # 固定链接，缺省用文件名
   ---
   ```
2. 按照【标准 B】将 `[配图：xxx]` 替换为 `{% image %}`（同时检测文件是否存在，不存在追加 `⚠️ 警告注释`），在核心重点处插入 1~3 个 `{% note %}`，适量使用 `{% box %}` / `{% mark %}`。
3. 保存文件，**【硬关卡 HK-2】** 在对话框提示：
   > *"阶段二 Stellar 语法润色完成！请在本地编辑器查看终稿，或在 http://localhost:3456/ 本地服务器预览确认实际渲染效果。"*
   > *"若满意请确认【是否执行发布与提交】；若需微调，请直接提出修改意见。"*
4. **【微调分支】**：若用户提出针对标签或样式的修改意见，在阶段二直接增量微调，确认无误后方可进入阶段三。

---

### 阶段三：Smart Git 评估、自动 Commit、Push 与发布善后
收到用户确认发布指令后：
1. **【本地构建验证（HK-3）】**：Push 前先在终端运行 `npm run build`（hexo generate）做本地构建验证。若构建报错则先修复、不得直接提交。若构建通过但改动未生效（hexo 缓存问题），先运行 `npm run clean` 清理缓存后重新构建验证。
2. **【Smart Git 评估】**：自动在终端运行 `git status --short` 扫描所有变动文件（注：`node_modules/`、`public/`、`db.json`、`*.log`、`.deploy_git/` 等已被 `.gitignore` 忽略，通常不会出现在变动列表）：
   - **分支 A（精准提交）**：若检测到存在未完成草稿、无关杂物（`.gitignore` 之外的新增临时文件等），AI **禁止**使用 `git add .`，仅精准添加：
     `git add source/_posts/本次文章.md source/images/相关图片`
   - **分支 B（全量提交）**：若所有改动均为合法博客资产（如本次文章 + 图片 + 顺带修改的 `_config.stellar.yml`），且无危险杂物，自动选用 `git add .`。
3. **【自动 Commit 与 Push】**：
   - 生成规范提交信息：`feat(post): 发布文章《[文章标题]》`
   - 自动在终端**按顺序分步执行**（禁止用 `&&` 连成一行——Windows PowerShell 5.1 不支持 `&&`，跨 AI 工具执行会报错）：
     1. `git commit -m "feat(post): 发布文章《[文章标题]》"`
     2. 确认 commit 成功后，再单独执行 `git push`
4. **【Vercel 构建提醒与草稿清理】**：
   - Push 成功后在对话框告知用户：
     > *"已成功为您自动 Push 至 GitHub！Vercel 正在自动构建上线。"*
     > *"检测到根目录下存在原始临时草稿 `[草稿文件名.md]`，是否需要我为您清理删除？"*
   - 收到删除确认后，自动执行文件删除，完成本次任务。

---

## 四、 版本与回滚兜底
- 项目本身由 Git 管理，每阶段写入前可用 `git status` 复查，若某阶段产物有误，可用 `git checkout` / `git restore` 回滚至上一安全状态，不必人工修复。
- 每次修改博客文件前，如无把握，可在工作区先留存副本，降低不可逆风险。

---

## 五、 规则冲突处理与优先级
当文档内多条规则发生冲突或表述不一致时，按以下优先级裁决（从高到低）：
1. **核心红线（第一章）**：最高优先级，任何规则不得与之冲突，违反即停。
2. **标准 C（表达优化方法论）**：作为正式指令，优先于阶段流程与排版细节标准；但须服从红线的保真约束。
3. **四阶段流程（第三章）**：阶段划分、执行顺序与硬关卡（HK-0~3）。
4. **标准 A / 标准 B**：排版细节标准。
若按上述仍无法裁决，或指令存在歧义，一律停止并先询问用户，不得擅自决定或行动。
